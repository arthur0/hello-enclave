enclave {
    from "Sealing/Sealing.edl" import *;
    trusted {
        /* define ECALLs here. */
        public int ecall_generate_random_number(void);
    };

    untrusted {
        /* define OCALLs here. */
        void ocall_print([in, string]const char* str);
    };
};
